# Data Pipeline

Demonstrates a scheduled agent that runs daily ETL processing.
Shows cron-based scheduling with timezone and resource constraints.

Run: `agent validate .` from this directory to see a clean validation pass.
Run: `agent pack .` to produce the .agent file.
