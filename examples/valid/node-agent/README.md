# Web Scraper (Node.js)

Demonstrates a Node.js agent. The .agent format is language-agnostic -- the
manifest schema is identical regardless of runtime language.

Run: `agent validate .` from this directory.
Run: `agent pack .` to produce `web-scraper-1.0.0.agent`.
