# Customer Onboarding

Demonstrates a triggered agent that guides new signups through an onboarding
workflow. Uses LLM integration with model preferences.

Run: `agent validate .` from this directory to see a clean validation pass.
Run: `agent pack .` to produce the .agent file.
