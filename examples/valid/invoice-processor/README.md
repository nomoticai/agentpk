# Invoice Processor

Demonstrates a full-featured .agent manifest using every Zone 1 field.

Scheduled execution, multi-tool capabilities, data permissions, resource limits,
and environment constraints are all shown here.

Run: `agent validate .` from this directory to see a clean validation pass.
Run: `agent pack .` to produce `invoice-processor-2.1.0.agent`.
