spec_version: "1.0"
name: memory-missing-component
version: "1.0.0"
description: "Demonstrates missing AIR component file."
author: "agentpk examples"

runtime:
  language: python
  language_version: "3.11"
  entry_point: src/agent.py
  entry_function: main
  dependencies: requirements.txt

capabilities:
  tools:
    - id: scan_transaction
      description: "Read transaction data from the incoming event stream."
      scope: read
      required: true

execution:
  type: on-demand
