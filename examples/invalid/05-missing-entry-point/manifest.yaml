spec_version: "1.0"
name: "missing-entry-point"
version: "1.0.0"
description: Entry point file does not exist on disk.
runtime:
  language: python
  language_version: "3.11"
  entry_point: agent.py
  dependencies: requirements.txt
execution:
  type: on-demand
